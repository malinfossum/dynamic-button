## Assignment

Create a function that takes:

- button text
- background color
- text color

The function should:

- create a `<button>` using the provided values
- add the button to the page
- be reusable so that multiple buttons can be created.
